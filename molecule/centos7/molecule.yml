---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: .yamllint

platforms:
  - name: centos7
    groups:
      - openjdk
    image: ${DOCKER_IMAGE_BASE:-centos:7}

provisioner:
  name: ansible
  inventory:
    group_vars:
      openjdk:
        java_open_jdk_version: ${JDK_VERSION}
  lint:
    name: ansible-lint
scenario:
  name: centos7
verifier:
  name: goss
  lint:
    name: yamllint
