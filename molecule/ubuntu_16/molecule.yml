---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: .yamllint

platforms:
  - name: openjdk-ubuntu16
    groups:
      - openjdk
    image: ${DOCKER_IMAGE_BASE:-amd64/ubuntu:xenial }

provisioner:
  name: ansible
  inventory:
    group_vars:
      openjdk:
        java_open_jdk_version: ${JDK_VERSION}
  lint:
    name: ansible-lint
scenario:
  name: ubuntu_16
verifier:
  name: goss
  lint:
    name: yamllint
