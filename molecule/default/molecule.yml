---
dependency:
  name: galaxy
driver:
  name: docker

lint: |
    yamllint .
    ansible-lint .

platforms:
  - name: openjdk-stretch
    groups:
      - openjdk
    image: ${DOCKER_IMAGE_BASE:-debian:stretch-slim}

provisioner:
  name: ansible
  inventory:
    group_vars:
      openjdk:
        java_open_jdk_version: ${JDK_VERSION}
scenario:
  name: default
verifier:
  name: ansible
